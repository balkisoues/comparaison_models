#Fairness considerations !!

1. Same data / preprocessing for both models.

2. make MLP and RNN have similar number of parameters.

3. Same training regime: same optimizer, same learning rate schedule, same number of epochs, same batch size etc 

4. Report evaluations metrics.

